<section>
  <%= link_to(image_tag(project.image,width:'300'), {:controller => 'projects', :action=>project.path}) %>
  <article>
    <h2>
      <%= link_to(project.title, {:controller => 'projects', :action=>project.path})%>
    </h2>
    <p><%= project.description %></p>
  </article>
</section>